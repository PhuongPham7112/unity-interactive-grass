using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GrassModel : MonoBehaviour
{
    [SerializeField] public float m_grassDensityValue = 1.0f;
    [SerializeField] public float m_grassHeight = 1.0f;
    [SerializeField] public float m_grassWidth = 1.0f;
    [SerializeField] public float m_grassStiffness = 0.1f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
